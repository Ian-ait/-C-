> **文档状态：历史方法与实验记录**
>
> 当前代码的主说明请阅读 [`problem3_code_guide.md`](problem3_code_guide.md)。本文件保留早期建模说明、诊断过程和历史实验结果，其中标记为“修复前”的数字不代表当前 `grid-boundary` 或 `net-hgb` 配置的正式全年结果。

# C题问题3：三时间尺度因果滚动随机MPC

## 1. 任务与实际实现概览

本项目已实现“0点日前计划—6/12/18点购电调整—每10分钟储能反馈”的三时间尺度控制器。程序每天0:00生成144个10分钟时段的全天初始计划 `g0`；被策略启用的6:00、12:00、18:00节点使用最新预测重算，但每次只正式锁定到下一个启用更新节点前的交付区间；每个10分钟时段的真实负荷和光伏到达后，再由因果DP执行器决定真实充放电、紧急购电和弃光/无法利用电量。

实现方法是“两阶段补救近似 + 因果滚动随机MPC”，不是严格多阶段场景树，也不声称得到严格多阶段全局最优解。0:00的共享 `g0` 确实把未来允许调整的时段和对应费用纳入优化；情景相关补救变量提供未来调整机会的近似价值。该补救近似仍比严格非预见场景树乐观，这是当前模型的主要限制之一。

当前代码的默认真实执行策略为 `grid-boundary`。它是在原100 kWh SOC价值网格上增加连续实际SOC、精确能量平衡边界动作和线性价值插值后的修复版本。旧的块末SOC跟踪策略和第一轮一致价值策略仍分别保留为 `old-target`、`consistent-value`，用于复现实验和定位误差。本文后半部分保留的全年数字是在修复前生成的历史结果；本轮没有重新运行全年，也没有覆盖正式 `result3.xlsx`，因此这些历史全年数字不能代表当前 `grid-boundary` 策略的正式结果。

## 2. 与问题2的区别

问题2的整日随机计划和历史整轨迹抽样思路可以复用，但问题3增加了三个关键层次：

- `g0` 是0:00提交的全天初始购电计划，所有调整费用始终相对当天 `g0` 结算；
- 6:00、12:00、18:00的远期解只是 `proposed_g`，只有进入本次至下一更新之间的交付块才成为 `committed_g`；
- 情景中的储能变量只用于规划，真实动作不复制情景解，而是在真实值到达后由10分钟反馈器生成。

问题2代码中的数据读取、全日残差轨迹和储能状态转移思路经过时序审查后被重新实现；没有修改 `solve_problem2.py`、其副本或任何附件。

## 3. 只读项目审计结果

编码前实际检查到的核心文件如下：

- `附件/附件1.xlsx`：`Sheet1`，144×4数据；字段为时间、电价、小区负载、光伏发电预测功率；首末时刻为00:10和`0:00+1`。
- `附件/附件2.xlsx`：工作表 `小区负载`、`光伏发电实际功率`；两表均为365×145数据，日期为2025-01-01至2025-12-31，144个时段从00:10至`0:00+1`。
- `附件/附件3.xlsx`：`Sheet1`，1460×26数据；字段为日期、预报时刻、预报1小时至预报24小时；每天4条0/6/12/18点预测。日期列有1095个向下省略单元格，程序先 `ffill`。
- `附件/附件5/result3.xlsx`：4个工作表，分别为 `计划购电量`、`调整购电量`、`充放电量`、`紧急购电量`。前两表为335×147，覆盖2月1日至12月31日。
- 问题2可复用结构：宽表读取、固定电价读取、连续历史残差轨迹抽样、HiGHS线性规划、SOC网格DP、守恒检查和提交表结构。

发现一个模板与强制口径的冲突：模板前两表第一个时段标题为`0:10-0:20`，最后一个为`0:00-0:10+1`，整体比附件1/2和“00:10表示(00:00,00:10]”的口径晚一格。程序不修改官方模板标题，而把第1至第144个数值按列序映射到真实slot 0至143；内部CSV和账本使用无歧义区间，如 `(00:00, 00:10]`。该冲突也记录在 `run_config.json`。

## 4. 时间索引与单位

内部统一用区间结束时刻表示交付时段。slot 0的目标时刻为00:10，对应 `(00:00,00:10]`；slot 35的目标时刻为06:00；6:00更新只能影响slot 36，即从 `(06:00,06:10]` 开始。12:00和18:00同理。

附件中的功率全部先转换成每10分钟电量：

\[
E_t=P_t\times\frac{10}{60}=\frac{P_t}{6}\quad(\mathrm{kWh}).
\]

电价保持元/kWh，因此电量乘电价得到元。

## 5. 信息边界与walk-forward场景

规划函数只接收预测场景、当前SOC、固定价格、策略和当天 `g0`，不接收尚未发生的当天真实负荷或真实光伏。当天真实数组只在规划求解完成后切出当前交付块，传入执行器逐时结算。

负荷基准预测使用决策日期以前最多30天的同槽均值；历史不足时使用附件1曲线冷启动。6/12/18点可以使用当天已经结束时段的负荷误差中位数做指数衰减偏差修正，不能使用当前决策后的真实值。

场景不是对144个时段独立采样。程序按相同发布时间从历史记录中建立完整24小时的负荷与光伏联合残差轨迹，再整轨迹抽取并叠加到当前基准预测，因此保留时间相关性以及负荷/光伏之间的同期关系。候选轨迹必须同时满足：

\[
\text{source issue}<\text{decision time},\qquad
\text{source issue}+24\mathrm{h}\leq\text{decision time}.
\]

第二个条件确保历史轨迹的最后目标时刻也已经成熟。窗口随时间向前滚动，仅使用决策日期/时刻以前的数据。历史为空时退化为单条基准场景，并在审计表中记录场景数为1。

反馈价值函数对多个场景的当前缺口费用使用概率加权平均：

\[
\mathbb E[C_t]=\sum_s\pi_s C_{t,s},\qquad \sum_s\pi_s=1.
\]

实现中使用 `np.tensordot(probabilities, shortage, axes=(0,0))`，不是把各场景费用直接相加，并在进入DP前检查概率非负、维度匹配且合计为1。2025-06-20四个决策节点的审计结果均为8个场景、每个概率0.125、概率和1.0。

## 6. 光伏插值

附件3每条记录给出未来24个整点功率。决策时刻已观测的实际光伏作为0小时锚点，`预报1小时`至`预报24小时`作为1至24小时锚点，用线性插值生成每10分钟功率，再除以6得到电量。跨日目标用绝对时间索引处理。2025-01-01 0:00没有前一交付区间，按午夜光伏为0初始化锚点。

## 7. 规划变量与约束

规划LP包含共享初始计划或当前提交块购电、情景相关未来购电、充电、放电、SOC、紧急购电、多余电量，以及相对 `g0` 的向上/向下调整量。每个情景满足：

\[
g_t+d_t+e_t=L_t-V_t+c_t+w_t,
\]

\[
S_{t+1}=S_t+0.9c_t-\frac{d_t}{0.9},
\]

\[
1200\le S_t\le10800,\qquad
0\le c_t,d_t\le833.333333.
\]

不允许售电，所有购电和动作变量非负。LP先使用连续变量，并加入 `epsilon=1e-4` 元/kWh的吞吐惩罚。程序自动报告 `min(charge,discharge)` 的最大值；实际全年基线和单日8组合验证均为0，因此没有升级为MILP。

## 8. 目标函数与结算

当天0:00计划费用为 `p_t g0_t`。最终购电量相对同一 `g0` 只结算一次：

\[
a^-_t=\max(g^0_t-g_t,0),\qquad
a^+_t=\max(g_t-g^0_t,0).
\]

向下调整退回额以非负数保存，汇总时做减法。统一总费用为：

\[
C=\sum_t\left[p_tg^0_t-0.5p_ta^-_t+1.5p_ta^+_t
+5p_te_t+\epsilon(c_t+d_t)\right].
\]

CSV同时保存 `base_purchase_cost_yuan`、`downward_refund_yuan`、`upward_premium_yuan`、`emergency_cost_yuan`、`throughput_penalty_yuan`、结算费用和总费用。没有把最终购电再按普通电价重复收费。

## 9. 0/6/12/18点滚动流程

每个策略每天按下列顺序运行：

1. 0:00：读取当前SOC和0:00预测，求共享全天 `g0`。LP允许未来被启用更新覆盖的时段使用情景补救调整，因此 `g0` 考虑未来调整机会及费用。
2. 交付到下一更新前：购电量固定；每10分钟真实值到达后执行一次储能反馈并结算。
3. 6/12/18点（若策略启用）：历史时段不会进入优化；用当前真实SOC和新预测求解。只把到下一启用更新前的共同解写入 `committed_g`，更远部分只记为 `proposed_g`。
4. 所有调整仍相对0:00的 `g0` 结算，因此一个交付时段不会被连续节点重复收费。

`decision_ledger.csv`逐次记录 `decision_time`、`target_time`、`forecast_issue_time`、`g0_kwh`、`proposed_g_kwh`、`committed_g_kwh`、上次承诺和本次是否锁定。0:00的初始合同是全天 `g0`；日内节点只修改其本次锁定块。

## 10. 真实执行与紧急购电

规划情景动作不用于真实执行。对每个提交块，程序先基于整段相关场景反向计算SOC价值函数；当前10分钟的真实净负荷到达后，选择满足功率/SOC限制且“当前紧急购电费用 + 吞吐惩罚 + 后续价值”最小的状态转移。由于一次状态转移只能向上或向下，真实动作结构上不会同时充放电。

### 10.1 三种反馈策略

- `old-target`：保留修复前逻辑，以2元/(SOC-kWh)惩罚真实块末SOC偏离规划情景平均块末SOC。它只作为对照，不是当前默认方案。
- `consistent-value`：停用上述人为块末目标，改用与规划LP相同的线性终端SOC价值，并增加动作支配规则；但实际下一SOC仍只能取离散网格点，因此保留为第一轮修复对照。
- `grid-boundary`：当前默认方案。在 `consistent-value` 基础上保留离散网格存储未来价值，但允许真实SOC连续变化，并为当前能量平衡增加精确边界动作。

第一轮修复删除了真实DP中独立的2元块末SOC偏差惩罚。非年末反馈DP与规划LP使用相同的线性终端SOC价值；只有 `old-target` 显式保留旧惩罚以便复现。第一轮同时加入：

\[
e_t>0\Rightarrow c_t=0,\qquad
w_t>0\Rightarrow d_t=0.
\]

测试随后发现，第一条在题面中更接近“紧急购电只弥补实际运行不足”的语义，但电量平衡式本身没有明确禁止紧急购电参与储能。因此程序把它做成可配置规则：默认 `--forbid-emergency-charging`，也可以用 `--allow-emergency-charging` 做题意口径敏感性。第二条仍作为 `consistent-value` 和 `grid-boundary` 的固定动作支配规则。`old-target` 不强制这两条规则，以保留原始对照。

### 10.2 SOC网格量化及边界状态修复

未来价值函数继续存储在SOC网格上，默认步长为100 kWh。若只允许实际下一SOC取网格点，则一次最小正放电为：

\[
d_{\min}=\eta_d\Delta S=0.9\times100=90\text{ kWh}.
\]

当当前缺口小于90 kWh时，零放电会留下紧急购电；下降一个SOC网格又会过量放电并产生 `w_t>0`，随后被动作支配规则删除。这会让DP并非因为经济价值，而是因为动作集合不完备而被迫紧急购电。

`grid-boundary` 不缩小未来价值网格，而是在每个真实时段的候选集中额外加入当前连续SOC和精确平衡状态。记储能动作前净缺口为：

\[
D_t=L_t-g_t-V_t.
\]

若 \(D_t>0\)、放电功率和SOC均允许，则加入：

\[
d_t=D_t,\qquad S_{t+1}=S_t-\frac{D_t}{\eta_d}.
\]

若 \(D_t<0\)，令剩余电量 \(R_t=-D_t=g_t+V_t-L_t>0\)。当充电功率和SOC上限允许时，加入：

\[
c_t=R_t,\qquad S_{t+1}=S_t+\eta_cR_t.
\]

边界状态不强制舍入。若 \(S_{t+1}\) 不在SOC网格点上，程序用相邻有限网格点对 \(V_{t+1}(S_{t+1})\) 做线性插值。后续时段从该连续SOC继续生成可达网格状态、零动作状态和新的精确边界状态。因此实际SOC递推保持精确，100 kWh网格只控制未来价值近似精度，不再限制实际充放电量必须是90 kWh或其倍数。

程序在 `problem3_detail.csv` 中记录 `boundary_candidate_available`、`boundary_candidate_used`、下一时刻SOC边际价值、折算为每输出kWh的放电边际价值和当前紧急购电单价；正紧急购电时段另存为 `emergency_marginal_audit.csv`。

实际平衡为：

\[
g_t+V_t+d_t+e_t=L_t+c_t+w_t.
\]

`w_t` 在程序中命名为 `curtailment_or_unused_kwh`：它是不能被负荷或储能吸收的总多余电量，主要对应弃光，但在已经承诺购电且实际负荷偏低时也可能包含无法利用的合同电量，所以不能在所有时段都严格解释成纯光伏弃光。

## 11. 跨日SOC与终端价值

1月1日0:00从6000 kWh开始，之后每个日期直接继承前一天24:00的SOC。1月既用于在线模型初始化，也逐10分钟真实运行；没有在2月1日重置。

每次6/12/18点预测窗覆盖未来24小时，因此18:00优化显式包含次日信息。非年末规划LP和当前反馈DP都在各自预测窗口末端对SOC赋予：

\[
v_S=\eta_d\operatorname{median}(p)=0.9\operatorname{median}(p)
\]

的线性剩余价值。在最小化目标中写成 \(-v_S S_T\)，所以较高终端SOC降低目标值，符号与“库存具有正价值”一致。终端价值只在预测窗口末端加入一次，反向递推过程中不重复累计；本轮修复没有调整该系数。`old-target` 仍保留独立块末SOC目标作为历史对照，`consistent-value` 和 `grid-boundary` 不再追踪规划情景平均块末SOC。

只有2025-12-31 24:00使用所有策略共同的硬终端条件 `SOC=6000 kWh`。没有强制每日SOC回到日初值。当前局部测试没有覆盖年末硬终端；再次运行全年前仍需专门复核连续实际SOC在硬终端附近的插值可达性。

## 12. 消融与Shapley设计

代码实现全部 \(2^3=8\) 种更新组合：`000=S0`、`100=S06`、`010=S12`、`001=S18`、`110=S0612`、`101=S0618`、`011=S1218`、`111=S061218`。每个策略从相同初始SOC独立运行，并使用相同场景数、历史窗和种子。命令行也兼容题目中的别名 `S6=S06`、`Sall=S061218`。`--ablation`运行顺序边际所需的4种累计策略，`--all-subsets`运行全部8种并生成Shapley值。

时刻 \(i\) 的Shapley成本节约为：

\[
\phi_i=\sum_{A\subseteq N\setminus\{i\}}
\frac{|A|!(3-|A|-1)!}{3!}\,[C(A)-C(A\cup\{i\})].
\]

正值表示加入该预测时刻降低总费用；负值表示在给定近似策略和样本中反而增费。最终判断依据是含紧急购电和吞吐项的实际总费用，不以预测误差替代。

## 13. 程序结构

- `read_problem_data`：读取附件、字段/形状/日期审计、单位转换。
- `interpolate_pv_forecast`：24个整点预测插值为10分钟预测。
- `forecast_load`：walk-forward历史负荷曲线和已实现前缀偏差修正。
- `build_scenarios`：筛选成熟历史误差并抽取联合连续轨迹。
- `solve_stochastic_mpc`：构造并求解两阶段补救LP，检查HiGHS状态。
- `make_soc_grid`、`feedback_value_function`：构造离散SOC价值网格，并用概率加权场景费用反向递推。
- `interpolate_grid_value`：对连续实际SOC读取相邻网格的线性插值价值，不跨越不可达的无穷大区间。
- `discharge_marginal_value`：计算未来SOC的边际价值，并按放电效率折算为每输出kWh的机会成本。
- `execute_feedback_block`：在真实值到达后生成可达网格、零动作和精确平衡边界候选，选择因果真实动作。
- `simulate_strategy`：跨日SOC连续的计划、调整、执行、结算和账本。
- `validate_run`：完整性、时序、守恒、边界、费用、动作冲突和信息成熟性检查。
- `compute_shapley`：由8组合实际成本计算三时刻贡献。
- `write_result3`：复制模板并写入2月1日至12月31日结果。
- `save_run`：保存明细、账本、检查结果，并输出紧急购电时段边际价值审计表。

## 14. 运行命令

本机环境实际使用Anaconda Python 3.13，依赖为NumPy、pandas、SciPy、openpyxl，无新增安装。常用命令：

```powershell
# 1月1日单策略验证
python solve_problem3.py --data-dir ./附件 --out-dir ./analysis_outputs/problem3_smoke --date 2025-01-01

# 非1月1日未给initial-soc时，自动从1月1日因果回放到目标日
python solve_problem3.py --date 2025-02-01 --out-dir ./analysis_outputs/problem3_replay

# 显式SOC的孤立单日诊断（输出会标记diagnostic模式）
python solve_problem3.py --date 2025-06-20 --initial-soc 10300 --feedback-policy grid-boundary --out-dir ./analysis_outputs/diagnostic

# 从指定日期连续运行7天
python solve_problem3.py --date 2025-06-20 --days 7 --initial-soc 10300 --feedback-policy grid-boundary --out-dir ./analysis_outputs/diagnostic_7d

# 保留旧策略和第一轮修复策略作对照
python solve_problem3.py --date 2025-06-20 --initial-soc 10300 --feedback-policy old-target --out-dir ./analysis_outputs/old_target
python solve_problem3.py --date 2025-06-20 --initial-soc 10300 --feedback-policy consistent-value --out-dir ./analysis_outputs/consistent_value

# 若需检验“紧急购电是否允许参与充电”的题意口径
python solve_problem3.py --date 2025-06-20 --initial-soc 10300 --feedback-policy grid-boundary --allow-emergency-charging --out-dir ./analysis_outputs/allow_emergency_charging

# 以下全年命令是后续使用方式；本轮修复后尚未重新执行
python solve_problem3.py --all-year --strategy S061218 --out-dir ./analysis_outputs/problem3

# 全部8种组合及Shapley；策略规模较大，建议单独目录
python solve_problem3.py --all-year --all-subsets --out-dir ./analysis_outputs/problem3_ablation
```

命令行同时接受 `--output-dir` 和题目示例中的 `--out-dir`。`--feedback-policy` 默认为 `grid-boundary`；`--soc-step` 控制未来价值网格精度，但不会再把 `grid-boundary` 的实际SOC强行量化。路径均通过 `pathlib.Path` 处理，无机器绝对路径硬编码。

## 15. 已完成的实际运行结果

### 当前结果状态

本节分为两类结果：

1. “网格量化诊断与局部修复测试”来自当前 `grid-boundary` 代码，已重新运行单日、连续7天和2025年2月；
2. “正式全年基线”和“全年8组合”来自修复前旧反馈执行器，只保留为历史对照，尚未用当前代码重算，不能作为当前方案的正式最终费用。

本轮没有运行全年，也没有生成或覆盖新的正式 `result3.xlsx`。

### SOC网格量化只读诊断

第一轮 `consistent-value` 的2025年2月共有1673个紧急购电时段，紧急购电量61,348.09 kWh，单时段平均36.67 kWh。全部1673个时段的最终紧急购电量均小于100 kWh网格对应的最小正放电量90 kWh。按储能动作前的原始缺口 `D_t=L_t-g_t-V_t` 统计，其中772个时段满足 `0<D_t<90 kWh`。

进一步逐时段重建候选动作后发现：

- 1641个时段可以再下降一个100 kWh SOC网格，但会多放90 kWh并产生剩余电量，因此被 `w_t>0⇒d_t=0` 删除；
- 另有16个时段受网格与放电功率边界共同影响，只能通过小于一个网格步长的精确动作消除缺口；
- 合计1657个时段存在“精确边界动作可行，但原离散候选无法表达”的问题；其余16个时段确实受SOC或放电功率限制。

修复前正式旧结果的2月份也有1148个紧急购电时段，其中1131个时段的紧急购电量小于90 kWh。这说明量化误差原本已经存在，第一轮动作支配规则只是把它进一步放大。

### 修复前7天网格敏感性

在同一2025-06-20至2025-06-26、初始SOC=10,300 kWh、S061218、8场景、随机种子2025的条件下，仅改变 `consistent-value` 的SOC网格：

| SOC网格/kWh | 总费用/元 | 紧急购电量/kWh | 无法利用电量/kWh | 储能吞吐/kWh | 期末SOC/kWh |
|---:|---:|---:|---:|---:|---:|
| 100 | 406,529.23 | 15,657.41 | 78,387.22 | 198,740.00 | 10,700 |
| 50 | 370,512.94 | 8,400.50 | 70,787.82 | 213,632.78 | 10,800 |
| 20 | 347,416.71 | 3,514.68 | 66,365.76 | 223,579.33 | 10,720 |
| 10 | 339,777.68 | 1,838.08 | 64,429.21 | 227,304.11 | 10,760 |

网格变细时紧急购电量和总费用持续下降，确认动作量化是第一轮异常的主要原因。`grid-boundary` 在继续使用100 kWh未来价值网格的情况下，把同一7天紧急购电进一步降到100.31 kWh，说明无需把整个价值函数改成10 kWh网格即可解决大部分量化误差。

### 三种反馈策略的局部复测

单日和7天从2025-06-20开始，初始SOC均为10,300 kWh；2月测试从2025-02-01开始，三种策略使用相同初始SOC=8,500 kWh。全部使用S061218、8场景、100 kWh价值网格和随机种子2025。冲突列依次表示“紧急购电同时充电 / 多余电量同时放电”。

| 测试范围 | 反馈策略 | 总费用/元 | 紧急购电量/kWh | 无法利用电量/kWh | 储能吞吐/kWh | 期末SOC/kWh | 冲突时段 |
|---|---|---:|---:|---:|---:|---:|---:|
| 单日 | old-target | 40,355.71 | 579.07 | 27,951.70 | 24,253.17 | 8,600.00 | 10 / 26 |
| 单日 | consistent-value | 44,031.72 | 1,148.67 | 27,544.24 | 17,327.78 | 8,700.00 | 0 / 0 |
| 单日 | grid-boundary | 37,533.33 | 约0 | 26,464.49 | 19,556.19 | 8,395.48 | 0 / 0 |
| 连续7天 | old-target | 361,321.58 | 5,311.54 | 78,083.40 | 241,385.57 | 10,200.00 | 96 / 237 |
| 连续7天 | consistent-value | 406,529.23 | 15,657.41 | 78,387.22 | 198,740.00 | 10,700.00 | 0 / 0 |
| 连续7天 | grid-boundary | 332,951.05 | 100.31 | 63,247.19 | 230,107.38 | 10,656.95 | 0 / 0 |
| 2025年2月 | old-target | 1,565,516.20 | 26,198.26 | 283,321.09 | 1,004,146.51 | 8,000.00 | 310 / 802 |
| 2025年2月 | consistent-value | 1,746,334.40 | 61,348.09 | 285,610.94 | 861,983.33 | 8,700.00 | 0 / 0 |
| 2025年2月 | grid-boundary | 1,455,359.24 | 2,914.08 | 231,367.06 | 985,635.24 | 7,951.13 | 0 / 0 |

相对第一轮 `consistent-value`，`grid-boundary` 的2月总费用下降290,975.15元，即16.66%；紧急购电量减少58,434.01 kWh，即95.25%；无法利用电量减少54,243.88 kWh。2月共有3149个时段生成精确平衡边界候选，其中2962次被实际采用；4032个时段中有3000个期末SOC不是100 kWh网格点，证明真实SOC没有再被强制舍入。

### 剩余紧急购电与电池边际价值

`grid-boundary` 修复后，2月只剩55个正紧急购电时段、合计2914.08 kWh：

- 49个时段无法依靠当前电池完全覆盖原始缺口，受SOC下限或833.333 kWh/时段放电功率限制；这些时段的电池放电边际价值低于当期紧急购电单价；
- 6个时段具备完全消除缺口的精确边界候选，但DP仍选择保留SOC。其平均电池放电边际价值为5.2755元/输出kWh，平均紧急购电单价为5.0287元/kWh，边际价值高出0.2314至0.2499元/kWh，因此该选择来自当前未来价值判断，不是网格缺少小动作。

电池边际价值定义为下一时刻价值函数沿放电方向的局部斜率。若SOC边际价值为 `m_S` 元/(SOC-kWh)，则每输出1 kWh需要消耗 `1/eta_d` kWh SOC，折算后的放电边际价值为 `m_S/eta_d` 元/输出kWh。程序将它与当前 `5p_t` 同单位比较，并在 `emergency_marginal_audit.csv` 中逐时段保存二者及差值。

### 修复前正式全年基线（历史结果，尚未重算）

正式输出使用 `S061218`、8条场景、30天历史窗、随机种子2025、100 kWh SOC网格。实际回放365天、52,560个时段，耗时217.65秒。结果为：

| 指标 | 实际值 |
|---|---:|
| 总费用 | 19,147,419.88 元 |
| 初始计划基准费用 | 15,158,365.45 元 |
| 调整净费用 | 2,399,531.08 元 |
| 紧急购电费用 | 1,588,251.12 元 |
| 紧急购电量 | 433,185.37 kWh |
| 向上调整量 | 2,269,133.09 kWh |
| 向下调整量 | 2,085,123.34 kWh |
| 弃光/无法利用电量 | 3,898,913.10 kWh |
| 储能吞吐量 | 12,722,377.24 kWh |

1月31日末SOC与2月1日初SOC在现有正式明细中均为8500 kWh，证明2月没有重置。正式全年最后时刻SOC为6000 kWh，满足统一终端条件。上述数值属于修复前旧反馈执行器的历史结果。

### 修复前8组合单日端到端验证（历史结果）

在2025-01-01、相同初始SOC=6000、随机种子2025、4场景上实际运行了全部8种组合。该日历史尚为空，各策略实际使用同一条冷启动场景；因此这些数字用于验证流程和Shapley代码，不代表全年预测时刻价值。总费用依次为：S0 125,167.12、S06 110,696.48、S12 107,581.29、S18 111,320.09、S0612 97,832.08、S0618 99,856.87、S1218 107,771.37、S061218 98,022.16元。对应Shapley成本节约为：6点11,608.69元、12点9,209.03元、18点6,327.24元。

### 修复前全年8组合消融与边际价值（历史结果，尚未重算）

全年8组合采用相同的4场景、200 kWh网格、30天历史窗、随机种子2025和初始SOC=6000；每种策略均独立从1月1日回放，并共享同一终端条件。它是为完成组合比较而运行的较轻配置，不能把其中 `S061218` 的21,592,644.00元与上面的8场景/100 kWh正式基线19,147,419.88元直接作策略差异解释。

| 组合 | 总费用/元 | 调整净费用/元 | 紧急购电费用/元 | 紧急购电量/kWh | 弃光或无法利用/kWh | 储能吞吐/kWh |
|---|---:|---:|---:|---:|---:|---:|
| 000 / S0 | 22,470,796.70 | 0.00 | 5,070,274.87 | 1,125,976.55 | 7,360,407.22 | 11,164,583.68 |
| 100 / S06 | 23,099,786.37 | 2,410,938.19 | 5,498,609.14 | 1,194,216.95 | 5,894,405.02 | 11,682,958.05 |
| 010 / S12 | 22,006,466.51 | 1,504,364.10 | 4,839,371.19 | 1,071,366.15 | 5,392,584.42 | 12,127,392.46 |
| 001 / S18 | 22,066,449.84 | 2,072,260.80 | 3,739,648.78 | 883,958.15 | 6,265,421.69 | 11,234,678.46 |
| 110 / S0612 | 21,983,246.08 | 2,035,449.10 | 4,755,623.44 | 1,064,676.39 | 4,877,479.16 | 12,207,759.20 |
| 101 / S0618 | 22,081,655.13 | 3,374,918.59 | 3,537,777.48 | 843,923.77 | 5,442,843.64 | 11,662,366.44 |
| 011 / S1218 | 21,622,970.99 | 2,417,535.11 | 3,562,126.81 | 838,044.43 | 5,207,023.14 | 11,930,034.99 |
| 111 / S061218 | 21,592,644.00 | 2,940,278.83 | 3,488,403.24 | 833,401.92 | 4,682,788.67 | 11,934,641.77 |

按累计顺序 `000→100→110→111` 计算，6点、12点、18点的条件边际成本节约分别为-628,989.67元、1,116,540.29元、390,602.08元。负值表示只加入6点更新在该近似和参数下增加了调整/紧急购电综合成本，并非程序把“价值”强制为正。使用全部8个子集计算的Shapley成本节约为：6点 **-198,218.37元**、12点 **577,783.63元**、18点 **498,587.44元**；三者合计878,152.70元，等于 `C(S0)-C(S061218)`。在这组实际回放中，12点预测的平均合作贡献最大，18点次之，6点贡献为负，因此不能笼统断言每增加一次更新都会降低总费用。

完整数值（还包括初始计划费用、上下调整量和运行时间）保存在 `analysis_outputs/problem3/strategy_comparison_8_subsets_full_year.csv`，Shapley结果保存在 `shapley_values_full_year.csv`。

## 16. 自动检查结果

当前 `grid-boundary` 的单日、连续7天和2月三组测试全部通过：

- 紧急购电同时充电时段数均为0；
- 多余电量同时放电时段数均为0；
- 最大电量平衡残差不超过 `2.2737e-13 kWh`；
- 最大SOC递推残差不超过 `1.1369e-13 kWh`；
- 跨日SOC连续残差为0；
- 成本恒等式残差为0；
- `mature_history_only=true`，没有使用尚未成熟的历史误差或未来真实值；
- `historical_decision_lock_ok=true`，已经锁定的合同购电没有被后续节点修改；
- `uncommitted_proposals_do_not_change_commitment=true`，远期提案没有提前成为承诺。

下面的正式全年检查来自修复前历史基线，仅用于说明原输出文件在当时通过了守恒、时序和模板检查；它不能替代当前反馈策略的全年验证。

正式全年基线的 `checks.json` 全部通过：

- 365天、每天144时段，52,560条结算记录，无重复日期-slot；
- 所有数值有限，非负量最小值为0；
- 最大电量平衡残差 `3.1841e-13 kWh`；
- 最大SOC递推残差 `1.1369e-13 kWh`；
- 最大充电量833.333333 kWh，最大放电量833.141514 kWh；
- 实际同时充放电时段数0，规划LP最大同时充放电量0；
- 成本恒等式最大残差 `9.095e-13` 元（CSV往返后的浮点舍入量级）；
- 全部历史误差均在决策时刻前成熟；
- 跨日SOC连续残差0，年末终端SOC残差0。

工作簿验证结果：4个要求工作表均存在；计划和调整表均为335×147；334个日期行和144个时段值没有空白；指定日期2025-03-20、2025-06-20、2025-09-20、2025-12-20分别可在第49、141、233、324行提取。`充放电量`有2004条4小时块记录，`紧急购电量`按正紧急购电时段展开。

## 17. 输出文件

- 根目录 `result3.xlsx`：修复前正式全年基线提交表；本轮未覆盖，不能视为当前 `grid-boundary` 的新正式结果。
- `analysis_outputs/problem3/problem3_detail.csv`：完整时段执行、SOC与费用。
- `analysis_outputs/problem3/daily_summary.csv`：逐日汇总。
- `analysis_outputs/problem3/decision_ledger.csv`：计划、提案和承诺账本。
- `analysis_outputs/problem3/forecast_audit.csv`：情景来源、成熟性、求解状态和规划同时充放电审计。
- `analysis_outputs/problem3/checks.json`、`run_config.json`：自动检查和可复现参数。
- `analysis_outputs/problem3_feedback_fix/grid_sensitivity_pre_boundary/`：100、50、20、10 kWh网格的修复前7天敏感性结果。
- `analysis_outputs/problem3_feedback_fix/grid_boundary_fix/`：旧策略、第一轮修复策略和网格边界策略的单日、7天、2月对照结果。
- 各 `grid-boundary` 目录中的 `emergency_marginal_audit.csv`：正紧急购电时段的SOC、电池边际价值、`5p_t`和二者差值。

## 18. 假设、近似与未完成限制

- 0:00 LP是共享初始计划加情景相关补救的两阶段近似；情景补救知道整条抽样轨迹，没有构造严格多阶段非预见场景树，因此可能低估未来调整成本。
- 负荷预测采用透明、可复现的历史均值和短期偏差修正，没有训练更复杂模型。
- 历史情景从最多30天、同一发布时间的完整24小时成熟轨迹抽取；早期样本较少，1月1日只能使用确定性冷启动。
- `curtailment_or_unused_kwh` 是总多余电量，并非在所有时段都能严格拆成纯弃光与弃购电；若竞赛口径要求纯弃光，需要增加购电不可弃置约束或进一步分解变量。
- 未来SOC价值仍使用离散网格近似。`grid-boundary` 已解除实际动作和实际SOC的网格量化，并用线性插值读取未来价值，但价值函数本身仍可能随100 kWh步长产生插值误差。修复前正式基线为100 kWh网格，全8组合历史消融为200 kWh网格，二者不能直接作同精度成本排名。
- `e_t>0⇒c_t=0` 依据“紧急购电弥补运行不足”的题意语义默认启用，但平衡式没有显式排除紧急购电储能套利，因此程序保留 `--allow-emergency-charging` 作为口径敏感性；正式采用哪一口径应在论文假设中明确。
- 当前 `grid-boundary` 只完成单日、7天和2月验证，尚未测试2025-12-31硬终端附近的连续SOC可达性，也尚未重新生成全年费用、8组合Shapley和正式工作簿。
- CVaR未纳入正式基线。题目要求的实际总购电费用始终是主目标；代码当前也未实现CVaR敏感性，不能报告期望成本、CVaR和风险权重曲线。
- 没有启用MILP，因为吞吐惩罚后规划与实际执行均未发现明显同时充放电。
- 官方模板时段标题错位按列序兼容，内部时序和所有结算严格采用结束时刻口径。

## 19. 后续改进

下一步可构建带聚类/削减的严格多阶段场景树，并在6/12/18节点施加逐层非预见约束；改进概率负荷预测；将多余电量拆成纯弃光和合同电弃置；对场景数、历史窗、SOC网格和终端价值做系统敏感性；最后在保留实际总费用主指标的同时增加可选CVaR，并同时报告无CVaR基准、期望成本、CVaR、高分位成本和风险权重 `lambda`。
